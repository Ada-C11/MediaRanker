<h2>List of Works</h2>

<h4>Albums</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "album")
} %>

<h4>Books</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "book")
} %>

<h4>Movies</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "movie")
} %>

<a href="" class="btn btn-secondary">View top media</a>
<a href="" class="btn btn-primary">Add a new work</a>

