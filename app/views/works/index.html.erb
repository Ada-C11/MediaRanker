<h2>List of Works</h2>

<%= render partial: 'media_table', locals: { :category => "album", :title_name => "Albums" } %>

<%= render partial: 'media_table', locals: { :category => "book", :title_name => "Books" } %>

<%= render partial: 'media_table', locals: { :category => "movie", :title_name => "Movies" } %>

<%= link_to "View top media", root_path, method: :get, class: "btn btn-secondary" %>
<%= link_to "Add a new work", new_work_path, method: :get, class: "btn btn-primary" %>

