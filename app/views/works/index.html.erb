<h2>List of Works</h2>

  <%= render partial: 'media_table', locals: { :works => @albums, :title_name => "Albums" } %>

  <%= render partial: 'media_table', locals: { :works => @books, :title_name => "Books" } %>

  <%= render partial: 'media_table', locals: { :works => @movies, :title_name => "Movies" } %>

<%= button_to "View top media", root_path, method: :get, class: "btn btn-secondary" %>
<%= button_to "Add a new work", new_work_path, method: :get, class: "btn btn-secondary" %>

