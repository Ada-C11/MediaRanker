<h1>List of Works</h1>

<h3>Albums</h3>
<% @works.each do |work|%>
    <% if work.category == "album"%>
        <%= link_to work.title, work_path(work.id) %><br>
        <%= work.creator %><br>
        <%= work.publication_year %><br>
    <% end %>
<% end %>

<h3>Books</h3>
<% @works.each do |work|%>
    <% if work.category == "book"%>
        <%= link_to work.title, work_path(work.id) %><br>
        <%= work.creator %><br>
        <%= work.publication_year %><br>
    <% end %>
<% end %>

<h3>Movies</h3>
<% @works.each do |work|%>
    <% if work.category == "movie"%>
        <%= link_to work.title, work_path(work.id) %><br>
        <%= work.creator %><br>
        <%= work.publication_year %><br>
    <% end %>
<% end %>