<h3>List of works</h3>

<h4>Albums</h4>
<%= render partial: 'table', locals: { media: @albums } %>

<h4>Books</h4>
<%= render partial: 'table', locals: { media: @books } %>

<h4>Movies</h4>
<%= render partial: 'table', locals: { media: @movies } %>

<%= link_to "View top media", root_path, class:'button' %>

<%= link_to "Add a new work", new_work_path, class:'button' %>


<!--
<table>
  <thead>
    <tr>
      <th>Votes</th>
      <th>Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Upvote</th>
    </tr>
  </thead>
  <tbody>
    <% @works.each do |work| %>
      <tr>
        <td><%= work.votes.count %></td>
        <td><%= link_to work.title, work_path(work.id) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= link_to "Upvote", upvote_path(work_id: work.id), method: :post, class:'btn btn-success' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->






