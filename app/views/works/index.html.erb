<h1>Works</h1>
<br>
  <section class="works-table">
    <h4>Albums</h4>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Votes</th>
          <th scope="col">Title</th>
          <th scope="col">Artist</th>
          <th scope="col">Released</th>
          <th scope="col"></th>
        </tr>
      </thead>
      
      <% @albums = Work.where(category: "album") %>
      
      <% @albums.each do |album| %>
        <tr>
          <td>TBD</td>
          <td><%= link_to album.title, work_path(album.id) %></td>
          <td><%= album.author %></td>
          <td><%= album.publication_year %></td>
          <td><%= link_to "Upvote", class: "btn btn-primary btn-lg" %>
        </tr>
      <% end %>
    </table>
  </section>
  <br>

  <section class="works-table">
    <h4>Books</h4>
    <table class="table"> 
      <thead>
          <tr>
            <th scope="col">Votes</th>
            <th scope="col">Title</th>
            <th scope="col">Author</th>
            <th scope="col">Published</th>
            <th scope="col"></th>
          </tr>
        </thead>
         
      <% @books = Work.where(category: "book") %>
      
      <% @books.each do |book| %>
        <tr>
          <td>TBD</td>
          <td><%= link_to book.title, work_path(book.id) %></td>
          <td><%= book.author %></td>
          <td><%= book.publication_year %></td>
          <td><%= link_to "Upvote", class: "btn btn-primary btn-lg" %>
        </tr>
      <% end %>
    </table>
  </section>
  <br>
  
  <section class="works-table">
    <h4>Movies</h4>
    <table class="table"> 
      <thead>
          <tr>
            <th scope="col">Votes</th>
            <th scope="col">Title</th>
            <th scope="col">Producer</th>
            <th scope="col">Released</th>
            <th scope="col"></th>
          </tr>
        </thead>
         
      <% @movies = Work.where(category: "movie") %>
      
      <% @movies.each do |movie| %>
        <tr>
          <td>TBD</td>
          <td><%= link_to movie.title, work_path(movie.id) %></td>
          <td><%= movie.author %></td>
          <td><%= movie.publication_year %></td>
          <td><%= link_to "Upvote", class: "btn btn-primary btn-lg" %>
        </tr>
      <% end %>
    </table>
  </section>
  <br>
  
  <%= link_to "View Top Works", root_path, class: "btn btn-primary btn-lg" %>
  <%= link_to "Add a new work", new_work_path, class: "btn btn-primary btn-lg" %>

