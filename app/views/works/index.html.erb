<h2>List of Works</h2>

<h4>Albums</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "album")
} %>

<h4>Books</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "book")
} %>

<h4>Movies</h4>
<%= render partial: "table", locals: {
  list: Work.where(category: "movie")
} %>

<%= link_to "View top media", root_path, class: "btn btn-secondary"%>
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary"%>

