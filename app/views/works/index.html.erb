<main>
    <h2>List of Works</h2>

<section class="media-table">
    <h4>Albums</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Votes</th>
                <th>Title</th>
                <th>Created By</th>
                <th>Published</th>
                <th>Upvote</th>
            </tr>
        </thead>
        <tbody>
            <tr>
            <% @albums.each do |work|%>
                <td>
                <%= Vote.where(work_id: work.id).length %>   
                </td>
                <td>
                <%= link_to work.title, work_path(work.id) %>   
                </td>
                <td>
                    <%= work.creator %>
                </td>
                <td>
                    <%= work.publication_year %>
                </td>
                <td>
                    <%= button_to "Upvote", work_votes_path(work.id), method: :post %>
                </td>
            </tr>
        </tbody>
        <% end %>
    </table>
</section>

<section class="media-table">
    <h4>Books</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Votes</th>
                <th>Title</th>
                <th>Created By</th>
                <th>Published</th>
                <th>Upvote</th>
            </tr>
        </thead>
        <tbody>
            <tr>
            <% @books.each do |work|%>
                <td>
                <%= Vote.where(work_id: work.id).length %>   
                </td>
                <td>
                <%= link_to work.title, work_path(work.id) %>   
                </td>
                <td>
                    <%= work.creator %>
                </td>
                <td>
                    <%= work.publication_year %>
                </td>
                <td>
                    <%= button_to "Upvote", work_votes_path(work.id), method: :post %>
                </td>
            </tr>
        </tbody>
        <% end %>
    </table>
</section>

<section class="media-table">
    <h4>Movies</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Votes</th>
                <th>Title</th>
                <th>Created By</th>
                <th>Published</th>
                <th>Upvote</th>
            </tr>
        </thead>
        <tbody>
            <tr>
            <% @movies.each do |work|%>
                <td>
                <%= Vote.where(work_id: work.id).length %>   
                </td>
                <td>
                <%= link_to work.title, work_path(work.id) %>   
                </td>
                <td>
                    <%= work.creator %>
                </td>
                <td>
                    <%= work.publication_year %>
                </td>
                <td>
                    <%= button_to "Upvote", work_votes_path(work.id), method: :post %>
                </td>
            </tr>
        </tbody>
        <% end %>
    </table>
</section>

</main>

