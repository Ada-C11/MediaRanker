<h2> <%= @work.title %> </h2>
<p> Created by: <%= @work.creator %> </p>
<p> Published: <%= @work.publication_year%> </p>

<%= link_to "Back to Media Ranks", root_path %>
<%= link_to "Edit", edit_work_path(@work) %>
<%= link_to "Delete", work_path(@work), method: :delete, data: { confirm: "Are you sure you want to delete this #{@work.category}?" }%>

<% if @work.category%>
<h2> Votes for this <%= @work.category.capitalize %> </h2>
<% end %>