<section>
  <h2><%= @work.title %></h2>
  <p>Created by: <%= @work.creator %></p>
  <p>Published: <%= @work.publication_year %></p>
  <p><%= @work.description %></p>

  <%= link_to "Back to media ranks", root_path, :class => "btn btn-primary" %>
  <%= link_to "Edit", edit_work_path(@work), :class => "btn btn-primary" %>
  <%= link_to "Upvote", upvote_path(@work), method: :post, :class => "btn btn-primary" %>
  <%= link_to "Delete", work_path(@work), method: :delete, data: { confirm: "Are you sure?" }, :class => "btn btn-danger"%>
</section>

<section class="works-votes_container">
  <h3 class="works-votes_header"><%= "#{@work.votes.size} Votes for this #{@work.category.name}" %> </h3>
  <table class="table">
        <thead>
          <tr>
            <th>User</th>
            <th>Date</th>
          </tr>
        </thead>
        <% @work.votes.each do |vote| %>
          <tbody>
            <tr>
              <td><%= link_to vote.user.username, user_path(vote.user)%></td>
              <td><%= vote.created_at.to_date %></td>
            </tr>
          </tbody>
          <% end %>
      </table>
</section>
