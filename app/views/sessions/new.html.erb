<h2>Log In</h2>

<%= form_with model: @user, url: login_path, method: :post, class: "form" do |f| %>
  <div class="form-group">
    <%= f.label :username, 'Username' %>
    <%= f.text_field :username, class: 'form-control' %>
  </div>

  <%= f.submit 'Log In', class: "btn btn-primary" %>
<% end %>