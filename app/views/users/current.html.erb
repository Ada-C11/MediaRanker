<h2>User Summary: <% @user.username %></h2>
<p>Joined site <% @user.created_at %></p>

<section>
  <h3>Votes</h3>
  <table class="table">
    <thead>
      <tr>
        <th>Media Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Category</th>
        <th>Voted On</th>
      </tr>
    </thead>

  </table>
</section>


<%= link_to "See all Users", root_path, class: "btn btn-secondary" %>
<%= link_to "Back to Media List", works_path, class: "btn btn-primary" %>