<h2 >List of Users</h2>

<ul>
<% @users.each do |user| %>
  <li>
    <%= link_to user.username, user_path(user.id) %>
    Vote: <%= user.vote_ids.length %>
    Joined: <%= (user.created_at).to_date.to_formatted_s(:long) %>
  </li>
<% end %>
</ul>