<div class="alert"> TODO: ADD LINKS TO UPVOTE AND ACCURATE VOTE COUNT </div>
<%# remove the above code before final submission%>

<section class="spotlight">
  <h2 class="spotlight__header">
  <span class="spotlight__header--prefix">
    Media Spotlight
  </span>
  <% random_work = Work.all.sample%>
  <%= link_to random_work.title, work_path(random_work)%>
  </h2>
  </section>
</section>

<section class="top-ten-container">
  <section class="top-ten-list-container">
  <h2 class="top-ten-header"> Top Movies </h2>
  <% top_movies = Work.movies.sample(10) %>
  <ul class="list-group top-ten__list" >
    <% top_movies.each do |movie|%>
      <li class="list-group-item">
        <h4>
          <%= link_to movie.title, work_path(movie) %>, 
          <small> created by <%= movie.creator%></small>
        </h4>
        <p> Votes </p>
      </li>
    <% end %>
  </ul>
  </section>

  <section class="top-ten-list-container">
    <h2 class="top-ten-header"> Top Albums </h2>
    <% top_albums = Work.albums.sample(10)%>
    <ul class="list-group top-ten__list">
      <% top_albums.each do |media|%>
        <li class="list-group-item">
          <h4>
            <%= link_to media.title, work_path(media) %>, 
          <small> created by  <%= media.creator%></small>
          </h4>
          <p> Votes </p>
        </li>
      <% end %>
    </ul>
  </section>
    
  <section class="top-ten-list-container">
  <h2 class="top-ten-header"> Top Books </h2>
  <% top_books = Work.books.sample(10)%>
  <ul class="list-group top-ten__list">
  <% top_books.each do |media|%>
        <li class="list-group-item">
          <h4>
            <%= link_to media.title, work_path(media) %>, 
            <small> created by <%= media.creator%></small>
          </h4>
          <p> Votes </p>
        </li>
      <% end %>
    </ul>
  </section>
</section>

