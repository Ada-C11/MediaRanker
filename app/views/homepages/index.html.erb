<h1>Homepages#index</h1>
<p>Find me in app/views/homepages/index.html.erb</p>

<h2>Media Spotlight: <% top = @works.sample %><%= link_to "#{top.title}", work_path(top.id) %> by <% top.creator %> </h2>
<p> votes placeholder - <%= top.description %></p>

<h2 class="top-ten__header">Top Movies</h2>

<% @works.where(category: "movie").each do |movie| %>
<%= movie.title %>
<% end %>

<h2 class="top-ten__header">Top Books</h2>
<% @works.where(category: "book").each do |book| %>
<%= book.title %>
<% end %>

<h2 class="top-ten__header">Top Albums</h2>

<% @works.where(category: "album").each do |album| %>
<%= album.title %>
<% end %>
